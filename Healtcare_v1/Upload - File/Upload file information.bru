meta {
  name: Upload file information
  type: http
  seq: 2
}

post {
  url: http://localhost:1339/api/upload?id={id}
  body: multipartForm
  auth: bearer
}

params:query {
  id: 
}

auth:bearer {
  token: {{token}}
}

body:multipart-form {
  fileInfo: 
  files: 
}
